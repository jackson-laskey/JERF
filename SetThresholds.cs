using UnityEngine;
using System.Collections;

public class SetThresholds : MonoBehaviour {


	/*public int numThresholds;

	public float[] thresholds;

	private GameObject thresholdParent;

	// Use this for initialization
	void Start () {
		thresholds = new float[numThresholds];
		thresholdParent = new GameObject ("Thresholds");
		thresholdParent.transform.SetParent (transform.parent);
		thresholdParent.transform.localScale = new Vector3 (1, 1, 1);
		for (int i = 0; i < numThresholds; i++) {
			thresholds [i] = (float)((float)i+1f) / (float)((float)numThresholds + 1f);
			GameObject threshold = Instantiate (Resources.Load ("Prefabs/Threshold"),
				new Vector3(transform.position.x, transform.position.y), Quaternion.Euler(0, 0, 0) ) as GameObject;
			threshold.transform.SetParent (thresholdParent.transform);
			threshold.transform.localScale = new Vector3 (1, thresholds[i], 1);
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
}
